# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                                      controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file                          controllers.Assets.at(path="/public", file)

GET         /authentication                        controllers.Authentication.login()
POST        /authentication                        controllers.Authentication.authenticate()
GET         /logout                                controllers.Authentication.logout()
# my ideas

GET         /users/new                             controllers.UserProfile.newUserForm()
POST        /users                                 controllers.UserProfile.newUser()
GET         /users/$id<[0-9]+>/confirm             controllers.UserProfile.confirm(id: Integer)
GET         /users/$id<[0-9]+>/personalData        controllers.UserProfile.newUserDataForm(id: Integer)
POST        /users/$id<[0-9]+>/personalData        controllers.UserProfile.newUserData(id: Integer)

GET         /offers                                controllers.Offers.all()
GET         /offers/new                            controllers.Offers.newOfferForm()
GET         /offers/$id<[0-9]+>/images             controllers.Offers.uploadForm(id: Integer)
POST        /offers/$id<[0-9]+>/images             controllers.Offers.upload(id: Integer)
POST        /offers                                controllers.Offers.newOffer()
GET         /offers/$id<[0-9]+>                    controllers.Offers.seeOffer(id: Integer)

#GET         /users/$id<[0-9]+>                      // user profile (with offers and bookings)
# oferty w profilu pojawiają się tylko przez miesiac od wygasniecia
#GET        /users/$id<[0-9]+>/offers // create offer form
#GET        /users/$id<[0-9]+>/offers/$id<[0-9]+>    // edit or create
#GET        /users/$id<[0-9]+>/offers/$id<[0-9]+>/export    // to pdf
#GET        /users/$id<[0-9]+>/offers/$id<[0-9]+>/images    // edit or add create // czyli wyswietl wszystki jakie sa i formularz dodawania
#POST        /users/$id<[0-9]+>/offers/$id<[0-9]+>/images    // edit or add create
#DELETE        /users/$id<[0-9]+>/offers/$id<[0-9]+>/images/:imId    // edit or add create

#POST        /users/$id<[0-9]+>/booking   // create

#PUT        /users/$id<[0-9]+>/offers/$id<[0-9]+>    // update
#DELETE     /users/$id<[0-9]+>/offers/$id<[0-9]+>    // remove
#POST       /users/$id<[0-9]+>/offers/$id<[0-9]+>    // create -- trzeba przeniesc



