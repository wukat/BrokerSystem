# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET         /                      controllers.Application.index()

# Map static resources from the /public folder to the /assets URL path
GET         /assets/*file          controllers.Assets.at(path="/public", file)

GET         /authentication        controllers.Authentication.login()
POST        /authentication        controllers.Authentication.authenticate()
GET         /logout                controllers.Authentication.logout()
# my ideas

GET         /newUser               controllers.UserProfile.newUserForm()
POST        /newUser               controllers.UserProfile.newUser()

#GET     /clients/:login              controllers.Clients.show(login :String)
#GET     /login                       controllers.Login.showForm()
#POST    /login                       controllers.Login.login()
